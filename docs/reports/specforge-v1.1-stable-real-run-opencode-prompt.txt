请在当前项目中执行一次 SpecForge v1.1-stable 真实运行验收。

目标：
创建一个最小网页文件 index.html，页面中只有一个 h1 标题，文本为 stable rc hello，字体颜色为 blue。

强制要求：
1. 必须走 SpecForge 工作流；
2. workflow_type=quick_change；
3. workflow_path=code_only_fast_path；
4. 必须从 WI created 开始，完整推进到 closed；
5. 必须经过 sf_user_decision_record、sf_merge_run、sf_code_permission、sf_changed_files_audit、sf_close_gate；
6. 不允许手写或直接修改 .specforge/work-items/** 下的治理产物；
7. 不允许通过 work_item.json.status 推进状态；
8. 不允许使用 development / review / implementation / done 等旧状态；
9. 如果遇到状态或审批证据冲突，必须 fail-fast，不得绕过；
10. 完成后说明 WI ID，并停止，不要做额外文件修改。
