/**
 * CAS module exports
 */

import { ContentAddressableStorage, CASBlobReference } from './ContentAddressableStorage';

// Re-export the class
export { ContentAddressableStorage };

// Re-export the interface as a type only
export type { CASBlobReference };
