{
  "name": "specforge",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "sf-install": "bun scripts/sf-installer.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "fast-check": "^4.7.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
